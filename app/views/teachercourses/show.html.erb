<div ng-controller="lessonCtrl">
  <% @teachercourse_array.each do | course | %>
  <div class="header-page">
    <h2><%= course.name %></h2>
  </div>
    <article class="type-system-sans course-content"> 
      <div class="vertical-tabs-container">
        <div class="vertical-tabs">
          <% course.chapters.each do |chapter| %>
            <a ng-click="fetchChapter(<%=chapter.id%>)" href="javascript:void(0)" class="chapter js-vertical-tab vertical-tab" rel="<%='chapter' + chapter.id.to_s %>"><%= chapter.name %></a>
              <% chapter.lessons.each do |lesson| %>
                <a ng-click="fetchLesson(<%=lesson.id%>)" href="javascript:void(0)" class="js-vertical-tab vertical-tab" rel="<%='lesson' + lesson.id.to_s %>"><%= lesson.name %></a>
              <% end %>
          <% end %>
            <a class="button add-chapter-btn" href="/chapters/new">  Chapter + </a>
            <a class="button add-lesson-btn" href="/lesson/new"> Lesson + </a>
        </div>
        <div class="vertical-tab-content-container course-tab-content-container">
          <div ng-bind-html="htmlElement">
            
          </div>

          <a href="#" editable-text="user.name">{{ course.name || "empty" }}</a>

        </div>
      </div>
   </article>
      <div class="course-toolbar">
        <br>
        <div class="course-toolbar-item header-drag">
          <img src="<%= asset_path 'header.png'%>">
        </div>
        <div class="course-toolbar-item text-drag">
          <img src="<%= asset_path 'text-field.png'%>">
        </div>
        <div class="course-toolbar-item image-drag">
          <img src="<%= asset_path 'image.png'%>">
        </div>
        <div class="course-toolbar-item video-drag">
          <img src="<%= asset_path 'video.png'%>">
        </div>
        <div class="course-toolbar-item file-drag">
          <img src="<%= asset_path 'file.png'%>">
        </div>
        <div class="course-toolbar-item sound-drag">
          <img src="<%= asset_path 'sound.png'%>">
        </div>
      </div>
  <% end %>
</div>

   
